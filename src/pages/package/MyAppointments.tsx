import React from 'react';
import { Navigate } from 'react-router-dom';

const PackageMyAppointments = () => {
  return <Navigate to="/my-appointments" replace />;
};

export default PackageMyAppointments;
